- name: "Install Ghostty using official install script"
  shell: |
    curl -fsSL https://raw.githubusercontent.com/mkasberg/ghostty-ubuntu/HEAD/install.sh | bash
  args:
    creates: /usr/bin/ghostty
  become: true
  tags:
    - ghostty
